#include <stdio.h>
#include <stdlib.h>

typedef struct v{
    int visitado;
    int lista_adj[100];
    int tamanho_lista;
    int distancia;
}v;

int main(){

    int q, v, i, x, y;

    v * vertice = malloc(sizeof(v)*(q+1));










    printf("\n");
    return 0;
}